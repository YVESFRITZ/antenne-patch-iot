/**
 * Réglages de la carte (Leaflet + tuiles libres).
 * La hauteur n'est plus définie ici : chaque conteneur parent impose la
 * sienne, et la carte la remplit entièrement.
 */
export const MAP_DEFAULTS = {
  zoom: 15,
  zoomWithMarkers: 14,
};
